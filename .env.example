# Optional — credentials can also come from the config file written by
# `npx -y @sniebauer/zendesk-mcp setup` (~/.config/zendesk-mcp/config.json).
# Env vars take precedence over the config file when both are set.
ZENDESK_SUBDOMAIN=
ZENDESK_EMAIL=
ZENDESK_API_TOKEN=
